apply plugin: 'com.android.application'

android {
    signingConfigs {
        ccsign {
            keyAlias '2048'
            keyPassword 'zr-12345'
            storeFile file('/Users/rz/Documents/github/android/2048/export.keystore')
            storePassword 'zr-12345'
        }
    }
    compileSdkVersion 22
    buildToolsVersion '19.1.0'
    defaultConfig {
        applicationId "z.cn.hula2048"
        minSdkVersion 10
        targetSdkVersion 19
        versionCode 1
        versionName "1.0"
        //ndk {
        //    moduleName "ai2"
        //    ldLibs "log", "z", "m"
        //    stl "stlport_static"
        //}
        signingConfig signingConfigs.ccsign
    }
    buildTypes {
        release {
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android.txt'), 'proguard-rules.pro'
        }
    }
    sourceSets.main {
        jni.srcDirs = []
        jniLibs.srcDir 'libs'
    }
    productFlavors {
    }
}

dependencies {
    compile fileTree(dir: 'libs', include: ['*.jar'])
    compile 'com.android.support:appcompat-v7:22.+'
}
